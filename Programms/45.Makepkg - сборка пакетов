###############################
## Makepkg - сборка пакетов ##
###############################

$ cd ~/Documents/install...  # переходим в директорию с файлами, которе нужно установить

$ makepkg -sic  # устанавливаем в систему

$ sudo pacman -U <название пакета>  # таким образом можно устанавливать пакеты .tar.xz (родной формат)

################
## Параметры ##
################

  makepkg  -A  # игнорировать неполную переменную 'arch' в 'PKGBUILD'
  
  makepkg -c  # удалять ненужные файлы после сборки
  
  makepkg -C  # удалять директорию '$srcdir/' перед сборкой пакета
  
  makepkg -d  # не проверять зависимости
  
  makepkg -e  # не извлекать исходные файлы (использовать существующие в директории '$srcdir/')
  
  makepkg -f  # переписать существующий пакет
  
  makepkg -g  # посчитать контрольные суммы исходных файлов
  
  makepkg -h  # показать справку и выйти
  
  makepkg -i  # установить пакет после сборки
  
  makepkg -L  # создать файл отчёта о процессе сборки
  
  makepkg -m  # отключить цветные сообщения
  
  makepkg -o  # только загрузить и распаковать исходные файлы
  
  makepkg -p <файл>  # использовать указанный скрипт для сборки вместо 'PKGBUILD'
  
  makepkg -r  # удалить установленные зависимости после сборки
  
  makepkg -R  # переупаковать содержимое пакета без пересборки
  
  makepkg -s  # установить недостающие зависимости с помощью 'pacman'
  
  makepkg -S  # создать архив с исходными файлами без загруженных файлов
  
  makepkg -V  # показать версию и выйти

  makepkg --allsource  # создать архив с исходными файлами с загруженными файлами
  
  makepkg --check  # выполнить функцию 'check()' из 'PKGBUILD'
  
  makepkg --config <файл>  # использовать указанный файл с настройками вместо '/etc/makepkg.conf'
  
  makepkg --holdver  # не обновлять исходные файлы в репозитории VCS
  
  makepkg --key <ключ>  # использовать указанный ключ для gpg-подписи
  
  makepkg --noarchive  # не создавать архив
  
  makepkg --nocheck  # не выполнять функцию 'check()' из 'PKGBUILD'
  
  makepkg --noprepare  # не выполнять функцию 'prepare()' из 'PKGBUILD'
  
  makepkg --nosign  # не создавать подпись для пакета
  
  makepkg --packagelist  # только вывести список пакетов которые будут созданы, без PKGEXT
  
  makepkg --printsrcinfo  # вывести сгенерированную SRCINFO и выйти
  
  makepkg --sign  # подписать получившийся пакет с помощью 'gpg'
  
  makepkg --skipchecksums  # не проверять контрольные суммы исходных файлов
  
  makepkg --skipinteg  # не проверять целостность исходных файлов
  
  makepkg --skippgpcheck  # не проверять PGP-подписи исходных файлов
  
  makepkg --verifysource  # загрузить исходные файлы (если необходимо) и провести проверки целостности

#################################################
## Эти параметры могут быть переданы 'pacman' ##
#################################################

  --asdeps  # установить пакеты как неявно установленные
  
  --needed   # не переустанавливать неустаревшие пакеты
  
  --noconfirm  # не спрашивать подтверждения при разрешении зависимостей
  
  --noprogressbar  # не показывать индикатор выполнения при загрузке

######################################################################
##Если параметр '-p' не указан, то makepkg будет искать 'PKGBUILD' ##
######################################################################
