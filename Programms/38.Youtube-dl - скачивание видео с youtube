##############################################
## Youtube-dl - скачивание видео с youtube ##
##############################################

$ sudo pacman -S youtube-dl  # установим утилиту

$ yaourt youtube-dl-gui-git  # графическая версия

$ youtube-dl http://www.ссылка-на-видео  # команда для скачивания видео (отдельные и плейлисты)

$ youtube-dl -F http://www.ссылка-на-видео  # команда для скачивания видео с выбором качества

$ youtube-dl -Acitf 22 http://www.youtube.com/playlist?list=ссылка-на-плейлист  # команда для скачивания плейлистов

    A     # автонумерация.
   
   -с    # в случае обрыва соединения, продолжит с момента обрыва, после повторного ввода команды в том же каталоге.
   
   -i    # игнорирование ошибок.
   
   -t     # назвать файл, так же как имя записи.
   
   -f 22    # выбор формата и качества (см. выше).

   —audio-format    # указать формат сохраняемого аудио файла (mp3, aac, wav, m4a, vorbis)
   
   —audio-quality     # качество аудио файла. 0 (лучше), 9 (хуже) для VBR или конкретный битрейт, например 128K (по умолчанию 5)

   -w    # не перезаписывать файл.
   
   -s    # симуляция скачивания. Файл не не скачивается и не сохраняется на жесткий диск.
   
   -x    # используем, если нужно скачать только звук.
   
   -o    # параметр указывает каталог для сохранения. По умолчанию программа скачивает в Домашнюю папку.
   
   -a     # возможность скачать группу файлов из текстового файла. Адреса нужных видео файлов вписываете в текстовый файл в таком порядке:

$ youtube-dl --list-extractors >>list  #команда для просмотра всех доступных сайтов, ск оторых можно скачать видео

$ youtube-dl -citw ytuser:<USER>  # скачать весь канал пользователя

$ youtube-dl -citw http://www.youtube.com/playlist?list=PLV3Gd8vEgOrjrME6jKGG3LCa0A4Qi9kDG --max-quality MP4  # максимальное качество видео для определенного формата

$ youtube-dl -ciw ytuser:hooplakidz -o '%(autonumber)s_%(title)s.%(ext)s' --autonumber-size 2  # по умолчанию программа переименовывает файлы, указывая название, а также идентификатор youtube видео в имени файла. Данная команда указывает только название и номер по порядку

$ youtube-dl -ciw ytuser:hooplakidz -o '%(uploader)s/%(autonumber)s_%(title)s.%(ext)s' --autonumber-size 2 --write-description  # эта команда дает возможность сохранять описания видео в файлах с расширением .description

$ youtube-dl -a video.txt  # команда для скачивания видео из файлика с ссылками