###########################
## Pacaur - альтернатива ##
###########################

$ yaourt pacaur  # установим альтернитивный пакетный мнеджер (альтернативный yaourt и pacman)

$ pacaur -Syu  # проверит и обновит пакеты, как по официальным репозиториям, так и по AUR

#####################################
## Специфично для оф. репозиториев ##
#####################################
                
$ pacaur -S <название пакета>  # установить пакет из оф. репозиториев

$ pacaur -Ss <название пакета>  # поиск по оф. репозиториям

$ pacaur -Si <название пакета>  # посмотреть информацию о пакете

$ pacaur -Sii <название пакета>  # посмотреть расширенное инфо о пакете 

$ pacaur -Sw <название пакета>  # загрузить пакет (без установки)

$ pacaur -Su <название пакета>  # установить пакет из кэша

$ pacaur -Qu  #

$ pacaur -Sc  # очистка кэша

$ pacaur -Scc  # полная очистка кэша
                   
########################
## Специфично для AUR ##
########################

$ pacaur -s <название пакета>  # поиск по AUR

$ pacaur -i <название пакета>  # посмотреть информацию о пакете
   
$ pacaur -d <название пакета>  # загрузить пакет(ы) (без установки)

$ pacaur -d -d <название пакета>  # загрузить пакет(ы) (с зависимостями из AUR)

$ pacaur -m <название пакета>  # загрузить и собрать пакет(ы) (makepkg)

$ pacaur -y <название пакета>  # загрузить, собрать и установить пакет(ы)

$ pacaur -k  # проверить обновления в AUR

$ pacaur -u  # обновить пакет(ы) из AUR

###########
## Общие ##
###########

$ pacaur -v  # показать информацию о версии pacaur

$ pacaur -h  # показать справку

###########
## Опции ##
###########

Ключи добавлять в конце

   -a  #  искать, собирать или устанавливать цели только из AUR
   
   -r  #  искать, собирать или устанавливать цели только из оф. репозиториев

   -e  #  редактировать файл PKGBUILD и просмотеть скрипт install
   
   -q  #  отображать меньше информации по запросу и поиску
   
   --devel  # учитывать обновление пакетов для разработчиков в AUR
   
   --foreign  # учитывать уже установленные внешние зависимости
   
   --ignore  # игнорировать обновление пакета (может быть использовано несколько раз)
   
   --needed   # не переустанавливать уже актуальные цели
   
   --noconfirm  # не запрашивать никаких подтверждений
   
   --noedit  # не выводить запрос на редактирование файлов
   
   --rebuild  # всегда пересобирать пакет(ы)
   
   --silent  # меньше вывода
