##########################
## Rsync - бэкап файлов ##
##########################

$ sudo pacman -S rsync  #утановим утилиту, которая обеспечивает быструю передачу данных

$ rsync -P -r /home/username/Музыка ~/Загрузки/backup  # делаем бэкап (по сути копируем из одного места в другое, для резервных копий)

$ rsync -P -r --exclude-from '/home/username/backup.txt'  /home/username ~/Загрузки/backup  # делаем бэкап всего того, что прописанно в файле "backup.txt"в директорию "Загрузки/backup". Можно как скрипт. (в папке "bin" скрипт "backup_rs.sh")

      -r  # рекурсивно

      --exclude-from  # что бэкапим

$ ~/.bin/backup_rs.sh  # команда запуска скрипта

$ sudo pacman -S cronie  # установим демон, для запуска скрипта по рассписанию (Планировщик заданий)

$ crontab -e  # откроется файлик для редактирования, куда пропишем (после первой строчки нажимаем "Enter". Обязательно!!!)

  */10 * * * * * /home/username/.bin/backup_rs.sh

#######################

*  # минуты (0-59)

*  # час (0-23)

*  # день (1-31)

*  # месяц (1-12)

*  # день недели (0-7)

#######################

$ crontab -r  # удалить задание

$ crontab -l  # посмотреть свои задания

#######################

$ sudo pacman -S grsync  # графическая утилита      
