########################
## Systemd - проблемы ##
#############################
## Изучение ошибок systemd ##
#############################

# попробуем определить проблемы при загрузке системы

$ systemctl  # список запущенных юнитов

$ systemctl --failed  # найдем службы systemd, которые не смогли запуститься

$ systemctl status название_unit  # попытаемся узнать побольше об обнаруженной проблеме (впишем название юнита). Запомним номер процесса:
   
   Process: 15630  # примерно будет выглядить так

$ systemctl restart название_unit  # посмотрим что даст нам перезагрузка службы, если проблема осталась, то идем ниже.

$ journalctl _PID=15630  # изучим более детально ошибку. PID - это номер процесса

$ ls -Al /etc/modules-load.d/  # если в результате предыдущей команды вы увидите, что речь идет об ошибке в конфиге, то можно узнать конкретнее в каком именно с помощью этой команды

$ /etc/modules-load.d/имя.conf  # редактируем конфиг,решанм проблему

$ systemctl start название_unit  # запустим службу после возможного решения проблемы

$ systemctl status название_unit  # снова посмотрим на статус службы

# Таким образом можно понять суть проблемы и уже оталкиваться от этого.  