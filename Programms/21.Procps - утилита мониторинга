##################################
## Procps - утилита мониторинга ##
##################################

$ sudo pacman -S procps-ng  # утилита мониторинга системы и процессов. (ин - интересные) Команды:

	free -h  # показывает количество свободной и занятой физической и виртуальной памяти системы (ин)

	top  # top позволяет отслеживать активность процессора в реальном времени (ин)

	w  # показывает информацию о зарегистрировавшихся пользователях и их процессах (ин)

	pwd  # отчет текущего каталога процесса (ин)

	sudo slabtop  # отображение информации о кеше ядра в режиме реального времени (ин)

    df -h /  # отображение  кол-во свободной и занятой памяти раздела

	pkill  # посылает сигналы процессам, основываясь на заданных именах и атрибутах

	kill  # посылает сигналы процессам
	
	pgrep  # выводит список процессов, основываясь на заданных именах и атрибутах
	
	pmap  # Возвращает  карту памяти указанного процесса. Ключ -d выводит значение
   используемой  writeable/private-памяти  - памяти
	
	ps    # выводит список процессов
	
	skill  # посылает сигналы процессам, попадающим под критерий
	
	snice  # меняет приоритет процесса, попадающего под критерий
	
	sysctl  # меняет параметры ядра во время его работы
	
	tload  # выводит статистику загрузки системы на указанный tty (или текущий, если не)
	
	uptime  # возвращает   время   работы   системы,   количество   вошедших  в  нее пользователей  и  загрузку системы
	
	vmstat  # возвращает   время   работы   системы,   количество   вошедших  в  нее пользователей  и  загрузку системы
	
	watch  # многократно выполняет команду, отображая ее вывод