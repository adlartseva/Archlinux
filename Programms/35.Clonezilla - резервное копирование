#######################################
## Clonezilla - резервное копироване ##
#######################################

Устанавливать и запускать livecd, т.к. разделы должны быть отмонтированны. Незабывае вставить вторую флэшку, куда будет записываться образ системы. или на свободный раздл жесткого диска

####################################################

$ sudo pacman -S clonezilla  # установим программу

$ sudo clonezilla  # запускаем программу и выбираем:

    device-image      # работать с дисками или разделами
    
    local_dev         # использовать локальное устройство
    
    /dev/sdb1         # выберим устройство, куда сохранять образ (флэшку)
    
    /                 # выбираем корневой каталог
    
    Beginner          # начальный уровень: принять настройки по умолчанию
    
    savedisk          # сохранить локальный диск как образ
    
    2016_05_15-img    # зададим название образа (любое)
    
    [*] sda           # выбрим диск, с которого хотим сделать резрвную копию
    
    пропустить проверку  # пропускаем проверку
    
    да, проверить сохраненный образ  # проверяем
    
    нет               # не шифровать диск

###############
## Iso бэкап ##
###############

$ mkdir ~/Clone  # создадим папку для нашего iso (все действия производим после бэкапа)

$ touch ~/Clone/hda1.iso  # создаем файлик, в дальнейшем он превратитсяв iso образ

$ sudo cat /Data/2016-24-05-16-img/sdb1.ext4-ptcl-img.gz.* | sudo gzip -d -c | sudo partclone.restore -C -s - -O /Data/clone/hda1.iso  # делаем из img быкапа - iso бэкап

$ cd ~/Clone/  # переходим в директорию с нашим iso образом

$ e2fsck -f hda1.iso  # проверяем файловую систему в iso + авто ремонт (подтверждаем, нажав "y")

$ resize2fs -f hda1.iso  # увеличиваем размер iso

$ sudo mount -o loop -t ext4 ~/Clone/hda1.iso /mnt  # смонтируем iso образ и проверим все ли на месте

