#Задаем права
sysmon :
- загрузка процессора
- загрузка памяти
- температура материнской платы (acpitz-virtual-0)
- температура графического ядра (nouveau-pci-0100)

sysmon-full-temp :
- загрузка процессора
- загрузка памяти
- температура материнской платы (acpitz-virtual-0)
- температура графического ядра (nouveau-pci-0100)
- температура Core 0 (первого ядра процессора)
- температура Core 1 (второго ядра процессора)

netspeed :
- скорость соединения

xgamma :
- меняет гамму цветов монитора

syndaemon :
- отключает тачпад через 2 сек. (-k -i 2) 
  после того, как вы начали печатать на клавиатуре

vebnout :
- запись с веб-камеры ноутбука себя (в левой нижнем углу экрана)  

vebpcdev :
- запись себя со сторонней вебки (device=/dev/video1)

audio_start :
- мелодия при старте

buckup_rs :
-бэкап файлов (rsync)

devilspie :
- автозапуск браузера свернутым

iron :
- информация о железе

system :
- информация о системе и ядре

thunar-convert-to-png.sh :
- конвертировать в png

thunar-convert-to-jpg.sh :
- конвертировать в jpg

thunar-media-info.sh :
- медиа инфо

thunar-video-to-gif.sh :
- преобразовать видео в gif

thunar-upload-to-imgur.sh :
- отправить в imgur

process :
- отобразить, запущен процесс (программа) в фоне
- # source process.sh thunar  # пример

ping :
- пинг

upgrub :
- обновить grub (добавить в /usr/bin)

ramup :
- узнать какие процессы используют всю ram

findf :
- поиск 10 самых больших файлов

ipfind :
- узнать IP

genpass :
- генерация случайного пароля

pacignor :
- список всех установленных пакетов, которые больше не от кого не зависят
